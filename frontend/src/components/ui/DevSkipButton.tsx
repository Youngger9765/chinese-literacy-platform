// Stub: 啟翔 PR #1087 imported but never committed the actual component.
// Added during Young's merge-from-staging so the build passes.
// Replace with real dev-mode skip button implementation in a follow-up PR.
const DevSkipButton = () => null;

export default DevSkipButton;
