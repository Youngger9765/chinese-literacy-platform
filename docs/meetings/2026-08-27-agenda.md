# 2026-08-27（四）早上 · 週會議程

> 上次：`docs/meetings/2026-08-21-record.md`
> 這份議程是**為了做決定**，不是為了報告狀態。狀態放在附錄，會上不逐條念。

## 0. 開場先講清楚的一件事（5 分鐘，Young）

8/26 Hans 實測回報四類 bug，全部已修並上 production。
但更重要的是**為什麼會漏掉**：

- 我（AI）先前用 `?p=` **直接開網址**驗證，而真實使用不會產生那種入口
- 判準只看「三篇內容不同」——**不同 ≠ 正確**
- 只驗了「讀全文」的 QR，沒驗「重點朗讀」的 QR

四類 bug 全部落在被標成綠燈的格子裡。已加上機器化的走查（見議題 A），
但這件事要在會上說一次，因為它會再發生在別人身上。

## A. 議題（要決定的事）

| # | 議題 | 現況（已查證） | 要決定什麼 | Owner |
|---|---|---|---|---|
| A1 | **交件日 8/31 剩 3 個工作天** | `2678` 平台說明書仍 OPEN | 說明書今天能不能收尾？收不完要不要縮範圍 | Hans |
| A2 | **9 月第一週現場 demo** | 上次決議「下週聯繫、目標 9 月第一週」 | 老師名單與日期是否已定？沒定的話今天定 | Hans |
| A3 | **B2B 漏斗 checklist** | 上次要求「要能直接給方大哥看」 | 做了嗎？沒做的話是否還需要 | Hans |
| A4 | **聚光燈根因**：抽取錯還是 render 錯 | 上次要求「分清楚哪一段回到舊版並開票」 | 結論是什麼；還沒查的話今天派人 | 啟翔 |
| A5 | **重點表驗證器 `2716`** | OPEN | 白名單機制做了沒？打中率目標 8～9 成是否維持 | 啟翔 |
| A6 | **重點段落抓太長** | 上次紅字項；本輪修的是「唸錯篇」不是「抓太長」 | 還在不在？要不要這輪處理 | 靖杭 |
| A7 | **朗讀三塊操作統一** | 上次決議全文／逐段／重點操作一致 | 做到哪；未登入能不能播（QR 那條已可） | 靖杭 |
| A8 | **`2595` / `2592` 上次說可直接 close** | 兩張都還 OPEN | 現在關掉還是留著 | Young |

## B. Sync：8/21 之後的更新（不逐條念，有疑問再問）

### B1. 一課多篇（#2916 / #2930）— 已上 production

有五課的學習單一份印了好幾篇課文（G5-L17、G6-L22、G8-L13、G9-L16、G9-L23）。
系統原本把整課當一篇，三篇的內容、朗讀、進度會混在一起。

Hans 8/26 回報並已修復：

| 回報 | 根因 | prod |
|---|---|---|
| 重點朗讀 QR 播課文第一段（9 張抽查） | 訪客頁換了內容，朗讀仍用「課號＋段落序號」對照，重點段不在那個索引裡 | ✅ 9/9 |
| 第 9/14（G6）、第 11（G5）步題目出現篇 1 | 模組叫 `vocab_application`，前端讀的欄位叫 `fill_in_blank` | ✅ |
| 第 11/16（G6）、第 13（G5）步重點表出現篇 1 | `/structure` 只帶課號，快取也只有課號 | ✅ |
| 按完成標記直接跳到最後一步 | 完成時傳的步驟 id 不含篇次，在序列裡找不到就跳報告頁 | ✅ |
| （自己抓到）三篇進度共用一個 key | 10 個步驟頁各自寫死步驟 id | ✅ |

**共同根因只有一句**：用課號定址，多篇課取不到自己那一份。
沒有錯誤、頁面正常、只有把三篇並排看才發現。

### B1.5 production 驗到哪（誠實界線）

| 路徑 | prod 實測 |
|---|---|
| 掃 QR 進來（訪客，老師發給學生的方式） | ✅ **9/9** 送出該課重點段 |
| 多篇課三篇的內容 | ✅ 三篇各自正確 |
| 登入後從第 1 步走完 20 步 | ⛔ **未驗** —— prod 停用一鍵登入，而**學生不能自助註冊**（後端 `block_student_self_registration`）。自建的是教師帳號，看到的課程頁不同。要驗得在 prod 建學校＋班級＋學生，那是往正式庫寫測試資料 → **要 Young 點頭** |
| 後台（`/admin/lesson-audio`） | ⛔ **未驗** —— 需要 admin 權限；註冊時帶 `role: admin` 會被後端忽略（這點是好的，沒有自封管理員的漏洞） |

登入後的完整 journey 在 **staging 五課全過**，而 prod 與 staging 是同一個 merge commit。
但那是推論，不是 prod 實測 —— 界線寫在這裡，不混進上面的綠燈。

### B2. 驗證方式改了（這是 A0 的下半）

新增 `tests/e2e/multiTextJourney.spec.ts`：登入後**從第 1 步逐步按完成標記走完**，
每一步跟後端該篇真值比對，並斷言高亮圓圈跟著篇次、進度 key 帶篇次、
不會中途跳報告頁。五課全跑，mutation 驗過（把比對指到別篇會紅）。

另外補了一條「模組 → 前端欄位」對照表的鎖 —— 那個形狀已經犯三次，
新增模組對照不上就會紅，不必等有人並排比對才發現。

### B3. 既有測試債清掉

22 條長期紅著的前端測試修完（`reading-steps` 全套 71 檔 546 條全綠）。
過程中挖到一個真 bug：**答錯時的「問 AI 助教」按鈕從 #2199 之後就再也沒出現過**
（條件互斥導致恆為 false）—— 學生答錯時求助入口是死的，已修。

## C. 上次 action items 閉環（附錄，只念沒動的）

| Owner | 事項 | 狀態 |
|---|---|---|
| Hans | `2678` 平台說明書 | ⏳ OPEN → A1 |
| Hans | `2595`／`2592` 直接 close | ❌ 兩張都還 OPEN → A8 |
| Hans | 聯繫四位老師 + `2821` 淑瑱老師 | ⏳ `2821` OPEN → A2 |
| Hans | B2B 漏斗 checklist | ❓ 未見產出 → A3 |
| Hans | demo 演練約 Young 一天 | ❓ 未約 → A2 |
| 啟翔 | 聚光燈查根因並開票 | ❓ 未見票 → A4 |
| 啟翔 | 重點表驗證器白名單 | ⏳ `2716` OPEN → A5 |
| 啟翔 | 清 issue `2153`/`2200`/`2425` | 部分完成：`2200`／`2425` 已關，`2153` 仍 OPEN |
| 靖杭 | 重點段落抓太長 | ❓ 未確認 → A6 |
| 靖杭 | 朗讀三塊操作統一 | ❓ 未確認 → A7 |
| 靖杭 | 逐段朗讀趨勢圖補回 | ❓ 未確認 |
| Young | `2839` 進度 PUT 沒發出去 | ✅ CLOSED |

⚠️ **Owner 集中度**：上次 11 項裡 Hans 佔 5、啟翔 3、靖杭 3。
若這次仍集中在同一人，那本身就是一個議題。

## D. 開學前提醒

（沿用上次，若無變化不念）
