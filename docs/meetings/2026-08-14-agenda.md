# 會議議程 2026-08-14（五）

參與者：Young、靖杭 @if-else-master、啟翔 @stgst ｜ 涵蓋 8/10~8/14

---

## 一、本週產出

- **Young｜12 個 PR，全在朗讀/TTS**（#2657~#2669）：段落一次合成、句間停頓 889→350ms、Azure 中斷改重試、IncompleteRead 改 requests（12%→0%）
  - ⭐ **#2667 值得共讀**：`import` 後同檔案再 `def` 同名函式會**靜默覆蓋**，前一晚的修改執行期一行都沒跑，而測試全綠
- **啟翔｜3 個 PR**（#2673 pilot 報告 / #2674 gate 修復 / #2675 課號對照表），已 merge 上 staging
- **靖杭｜本週無 PR**，最後一次 merge 是 8/03，至今 11 天

---

## 二、三件查證後跟原本認知不同的事

| | 結論 | 依據 |
|---|---|---|
| 1 | **Security Audit 恢復綠 → #2582 可關** | #2582 列的 4 個 high 被啟翔 PR 夾帶的 `npm audit fix` 解掉；staging 三個 SHA 皆 success |
| 2 | **上次標的最高風險不成立** | QR 綁 `lesson_id`(int, =story_id)，非會被重排的 `grade_code` → 重排不會讓已印的 QR 失效 |
| 3 | **#2573 是監控打錯路徑，服務沒掛** | `/health`=404 但 `/api/health`=200；frontend 200。改監控即可 |

但 **`lingoleap.tw` 真的壞著** —— `dig NS` 為空（#2529 未解）

> ⚠️ 第 2 點要立紅線：重排可改 `grade_code`/`display_order`，**永不可重用或重新指派既有 `story_id`**。
> 「新增 12 課」要配新 id、「總表有但線上無 13 課」不可拿舊 id 頂替 —— 動到就是所有已印的 QR 指錯課，紙收不回來

---

## 三、要決定的事

**A. #2561 二修教材課號校正（②完全卡這）**
178 筆待簽核：改序 130 / 改 code 23 / 新增 12 / 總表有但線上無 13（不變 9、線上多出 12）
→ 誰簽、什麼時候簽？「新增 12 課」story_id 誰配？「總表有但線上無 13 課」做不做？

**B. 卡最久 26 天的 4 個 PR**
啟翔 #2551 / #2552 / #2554（7/19-20 起）、Young #2567
→ 今晚排 review，還是關掉重做？繼續掛著是最差的

**C. 靖杭：11 天零產出 + 名下 18 張 open issue**
→ 要當面問是卡住、優先序不清、還是時間被佔走。**建議一起砍到 3 張**，不是加派工作

**D. lingoleap.tw 要不要救**
NS 為空是註冊/名稱伺服器層的事，不是 Cloud Run → 修，還是對外就用 `.run.app`？

**E. 啟翔個人 Gmail 公開在 PUBLIC repo**
`xiung.json` 的 email 是個人 Gmail（raymond 用 GitHub noreply）。同 8/04 移除就讀學校那類，當時漏了
→ 清空 / 換 noreply（要查正確 ID）/ 判定可接受。未自行改，猜錯 ID 會寫進錯地址

---

## 四、Action Items

| # | 項目 | Owner | 截止 |
|---|------|-------|------|
| 1 | 簽核 178 筆課號校正 | Young + 淑麗 | 8/18 |
| 2 | 定 story_id 分配規則（新增 12 課） | Young | 8/18 |
| 3 | 關 #2582，查 #2659/#2647/#2565 是否同根因 | Young | 8/15 |
| 4 | 修監控路徑 `/health`→`/api/health`（解 #2573 誤報） | Young | 8/15 |
| 5 | #2551/#2552/#2554 排 review 或關閉 | Young + 啟翔 | 8/15 |
| 6 | 靖杭 18 張 issue 收斂到 3 張 | Young + 靖杭 | 8/15 |
| 7 | 決定 lingoleap.tw 與 xiung.json email 兩件 | Young | 8/21 |
| 8 | 簽核後跑 `verify_qr_manifest` 再動 manifest | 啟翔 | 簽核後 |

---

> 本議程每條都會前實跑驗證（curl 帶負向對照、dig 帶正向對照、gh run 查現況），不採用狀態檔或舊註記
