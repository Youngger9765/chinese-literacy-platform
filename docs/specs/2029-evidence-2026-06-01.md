# #2029 Modular Spec System — Real Output Evidence

> 2026-06-01. 不是投影片、不是理論 — 這份是**實際跑出來的 terminal 輸出**，證明系統真的建好、真的會跑、真的抓到一個 production bug。每段都附**可重跑指令**。

## TL;DR — 真實作為

| 宣稱 | 真實證據 |
|------|----------|
| 系統建好且 merged | PR #2032 / #2033 merged，staging HEAD `8dbe10f0`、`4f93b087` |
| spec 契約會跑 | `pytest specs/` → **3 passed, 2 xfailed**（真實 session 輸出，見 §5）|
| registry 自動生成 + 防 stale | `build_registry.py --check` → `up to date`, exit 0（§4）|
| pilot 抓到真 bug | grader **364/398 (91%)** 字母解錯、**50/51 課**受影響（§8），具體錯例 §7 |
| 不是空殼 | grader 原始碼 `grep vocab_bank = 0`，真的用 `vocabulary[index]`（§6）|

---

## 1. 實際 merged commits（`git log --oneline -3`）

```
8dbe10f0 docs(meeting): 2026-05-29 OMO landing review record (issue 2029 capture) (#2033)
4f93b087 feat(specs): modular spec system Phase A + OMO pilot (#2029) (#2032)
6f1a9f11 docs(research): spec battle rounds 3-5 — codex rebuttal + claude response + critic verdict (#2031)
```

PR: https://github.com/Youngger9765/chinese-literacy-platform/pull/2032 (MERGED)

## 2. 檔案真的在 staging（`ls`）

```
specs/README.md
specs/build_registry.py
specs/registry.yaml
specs/ci/spec-check.yml
specs/modules/omo-assessment/INTENT.md
specs/modules/omo-assessment/probes/README.md
backend/specs/conftest.py
backend/specs/test_omo_assessment_spec.py
```

## 3. registry.yaml 真實內容（自動生成）

```yaml
# AUTO-GENERATED by specs/build_registry.py — DO NOT EDIT BY HAND.
modules:
- spec_id: omo.grader.letter_mapping
  module: omo-assessment
  canonical_source: vocab_bank
  owns_code:
  - backend/app/services/omo_question_schema.py
  owns_data:
  - backend/data/lessons/_parsed_2026-05-01/**/*.yml
  spec_tests:
  - backend/specs/test_omo_assessment_spec.py
  related_issues: [2015, 2027, 2028]
  source_meetings:
  - docs/meetings/2026-05-01-experts-review.md
  last_reviewed: 2026-06-01
  intent: specs/modules/omo-assessment/INTENT.md
```

## 4. registry 新鮮度 gate 真跑

```
$ python3 specs/build_registry.py --check
registry.yaml is up to date.
exit code = 0
```

（改 INTENT.md 不重生 → exit 1，drift 擋下；已驗證）

## 5. spec 契約真跑（`cd backend && python -m pytest specs/ -v`）

```
collected 5 items
specs/test_omo_assessment_spec.py::test_lesson_dir_exists PASSED         [ 20%]
specs/test_omo_assessment_spec.py::test_some_lessons_declare_vocab_bank PASSED [ 40%]
specs/test_omo_assessment_spec.py::test_vocab_bank_is_letter_to_word_dict PASSED [ 60%]
specs/test_omo_assessment_spec.py::test_every_fb_answer_letter_exists_in_vocab_bank XFAIL [ 80%]
specs/test_omo_assessment_spec.py::test_grader_resolves_letter_via_vocab_bank_not_vocabulary_index XFAIL [100%]
========================= 3 passed, 2 xfailed in 5.49s =========================
```

3 pass = 契約成立的部分；2 xfail = **記錄到的真實 drift**（修好會變 XPASS）。

## 6. grader 原始碼證明（不是猜的）

```
$ grep -c "vocab_bank" backend/app/services/omo_question_schema.py
0                          # grader 完全沒讀學習單的 vocab_bank

# _resolve_letter_answer 真實邏輯：
    idx = ord(letter.upper()) - ord("A")     # A=0, B=1 ... 用「順序」當索引
    v = vocabulary[idx]                       # 去 vocabulary 清單拿
    return str(v.get("word") or ...)          # 但 vocabulary 順序 ≠ vocab_bank
```

## 7. 具體錯例 — G6-L25（真跑 grader，非舉例）

```
學習單印給學生的 vocab_bank:        grader 實際算出來的:
   A = 揚帆啟航                       A → grader 說「股票」     ❌
   B = 股票                          B → grader 說「利潤」     ❌
   C = 節衣縮食                       C → grader 說「股東」     ❌
   D = 滿手老繭                       D → grader 說「積蓄」     ❌
   E = 集資                          E → grader 說「獲利」     ❌
```

學生圈 A（揚帆啟航），系統拿「股票」去比對 → 對的答案被判錯。

## 8. 全面 drift 量化（真跑全 51 課）

```
有 vocab_bank 的 lesson: 51 課
grader 解錯字母:        364/398 = 91%
受影響 lesson:          50/51 課（1 課順序剛好巧合對上）
fb answer 落在 vocab_bank 外: 5 個
```

> 原本 #2015 只當成「G6 四課 26/32」。spec module 一跑就揭露真實規模是 **G6+G7 全 51 課、91% 字母解錯**。這就是 modular spec 的實際作為：把一個被低估的 prod bug 量化清楚。

## 9. 同場一起做的真實產出

- `docs/meetings/2026-05-29-record.md` — 5/29 會議記錄（補上同事在等的；meeting-to-spec 閉環第一步）
- #2027 / #2028 issue 補充 — 把會議需求寫進 OMO issue（scanner 左右對照 / ground-truth corpus / edge case 清單）

## 10. 自己重跑（任何人可驗）

```bash
git checkout staging && git pull
python3 specs/build_registry.py --check          # registry fresh, exit 0
cd backend && python -m pytest specs/ -v          # 3 passed, 2 xfailed

# 重現 91% drift：
cd backend && python3 -c "
import yaml; from pathlib import Path
from app.services.omo_question_schema import _resolve_letter_answer
D=Path('data/lessons/_parsed_2026-05-01')
L=[f for f in sorted(D.glob('G[67]-L*.yml')) if isinstance((yaml.safe_load(f.read_text()) or {}).get('vocab_bank'),dict)]
tot=wrong=0
for f in L:
    d=yaml.safe_load(f.read_text()); vb=d['vocab_bank']; voc=d.get('vocabulary') or []
    for k,w in vb.items():
        tot+=1
        if _resolve_letter_answer(k,voc)!=w: wrong+=1
print(f'{wrong}/{tot} = {wrong*100//tot}% letters wrong across {len(L)} lessons')
"
```

## 誠實邊界

- spec system 本身是**真的、會跑、抓到真 bug**。
- 但它**沒有修** grader（grader 修動到 OMO 批改路徑 + 屬 #2015 assignee 靖杭的範圍）→ 我只用 xfail **記錄** drift，不越界改。
- CI workflow（`spec-check.yml`）因本機 token 缺 `workflow` scope **尚未自動裝**到 `.github/workflows/`，存成 `specs/ci/spec-check.yml` template 待裝 → 在那之前 spec 契約靠手動 / 本機跑，不是每 PR 自動擋。
