# 進度追蹤系統設計

> 記錄日期：2026-02-13
> 來源：LMS_EXTENDED_SYSTEMS.md 系統 2, 5
> Phase：1-2

---

## 📋 追蹤維度

### 1. 即時進度監控

**追蹤內容**：
- 作業完成率（班級/個人）
- 模組完成狀態
- 學習時間統計
- 正確率趨勢

**教師儀表板**：
- 全班進度一覽
- 落後學生預警
- 模組難度分析

**學生儀表板**：
- 個人學習日曆
- 進度條
- 能力雷達圖

---

### 2. 能力成長追蹤

**五大能力維度**（對應閱讀科學）：
1. **聲韻覺識** (Phonemic Awareness)
2. **識字解碼** (Phonics/Decoding)
3. **朗讀流暢度** (Fluency)
4. **詞彙能力** (Vocabulary)
5. **閱讀理解** (Comprehension)

**資料結構**：
```sql
student_abilities:
  - ability_id
  - student_id
  - ability_type (phonemic | decoding | fluency | vocabulary | comprehension)
  - score (0-100)
  - measured_at
  - measurement_source

ability_milestones:
  - milestone_id
  - ability_type
  - grade_level
  - milestone_name
  - threshold_score
```

**視覺化**：
- 雷達圖（五大能力）
- 成長曲線（時間軸 x 能力分數）
- 與同齡平均值比較

---

### 3. 學習活動日誌

**記錄事件**：
- `login` - 登入系統
- `start_assignment` - 開始作業
- `complete_module` - 完成模組
- `earn_badge` - 獲得徽章
- `level_up` - 升級
- `error_made` - 犯錯
- `retry_activity` - 重試

**資料結構**：
```sql
activity_logs:
  - log_id
  - student_id
  - event_type
  - event_data JSONB
  - session_id
  - timestamp
```

---

### 4. 學習行為分析

**分析維度**：
- 學習時段偏好（早上/下午/晚上）
- 學習習慣（集中 vs 分散）
- 困難容忍度
- 道具使用策略

**產出洞察**：
- 「小明習慣晚上 8-9 點學習，建議這時段推送新作業」
- 「小華遇到困難常放棄，建議降低初始難度」

---

### 5. 家長報告

**週報內容**：
- 本週學習時長
- 完成作業數
- 獲得徽章/升級
- 能力成長圖表
- 老師評語

**月報內容**：
- 月度總結
- 與上月比較
- 能力雷達圖
- 建議改進方向

**資料結構**：
```sql
parent_reports:
  - report_id
  - student_id
  - report_type (weekly | monthly)
  - period_start
  - period_end
  - summary_stats JSONB
  - teacher_comments
  - generated_at
```

---

## 📊 統計查詢範例

### 學生進步曲線

```sql
SELECT
  created_at::date AS date,
  AVG(accuracy) AS avg_accuracy,
  AVG(speed) AS avg_speed,
  AVG(fluency_score) AS avg_fluency
FROM reading_sessions
WHERE student_id = :student_id
  AND created_at >= NOW() - INTERVAL '30 days'
GROUP BY created_at::date
ORDER BY date ASC;
```

### 找出需要早期介入的學生

```sql
SELECT
  s.id,
  s.name,
  sp.last_practice_at,
  sp.reading_fluency_score
FROM students s
JOIN student_progress sp ON s.id = sp.student_id
WHERE sp.last_practice_at < NOW() - INTERVAL '7 days'
   OR sp.reading_fluency_score < 60
ORDER BY sp.last_practice_at ASC;
```

---

## 📚 相關文件

- [README.md](./README.md) - 功能總覽
- [../architecture/04-多租戶實作.md](../architecture/04-多租戶實作.md) - 機構級儀表板
