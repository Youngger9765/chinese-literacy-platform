# 技術棧決策

> **決策日期**: 2026-02-13
> **決策者**: 方大哥 + 開發團隊

---

## 🎯 最終技術棧

| 層級 | 技術選擇 | 版本 | 理由 |
|------|---------|------|------|
| **前端** | Next.js 14 (App Router) | 14.x | • 全端整合（API Routes）<br>• SSR/SSG 效能優異<br>• 成熟生態系<br>• TypeScript 完整支援 |
| **前端框架** | React 18 | 18.x | • Next.js 預設<br>• Hooks + Server Components |
| **前端樣式** | Tailwind CSS | 3.x | • 快速開發<br>• 設計系統友善 |
| **後端** | FastAPI (Python) | 0.109+ | • AI/ML 生態系最強<br>• Azure SDK 支援完整<br>• 文本處理庫豐富<br>• 未來 Whisper/GPT 整合容易 |
| **資料庫** | PostgreSQL | 15.x | • 關聯資料完整性<br>• GCP Cloud SQL 原生支援<br>• JSON 支援（靈活查詢） |
| **ORM** | SQLAlchemy (Python) | 2.x | • FastAPI 標準 ORM<br>• 型別安全<br>• 遷移工具成熟 |
| **語音辨識** | Azure Speech SDK (Python) | latest | • zh-TW 準確率高<br>• Python SDK 文檔完整<br>• 即時轉錄支援 |
| **AI 對話** | OpenAI GPT-4 Turbo | gpt-4-turbo | • Phase 3 蘇格拉底對話<br>• 理解度評估 |
| **部署** | GCP Cloud Run | - | • 無伺服器（Serverless）<br>• 按需計費<br>• 自動擴展 |
| **容器化** | Docker | - | • Cloud Run 需求<br>• 環境一致性 |
| **版本控制** | Git + GitHub | - | • 標準流程<br>• CI/CD 整合 |

---

## 🏗️ 架構設計

### 分離式架構（推薦）

```
┌─────────────────────────────────┐
│   Next.js 14 Frontend           │
│   Port: 3000                    │
│   - 學生端介面（朗讀練習）        │
│   - 教師端介面（班級管理）        │
│   - SSR/SSG 優化效能             │
│   - API Routes (BFF 層)         │
└──────────────┬──────────────────┘
               │
               │ HTTP/REST API
               │ (JSON)
               ↓
┌─────────────────────────────────┐
│   FastAPI Backend               │
│   Port: 8000                    │
│   - Azure Speech SDK 整合        │
│   - 文本比對演算法               │
│   - PostgreSQL CRUD             │
│   - GPT-4 Turbo 整合 (Phase 3)  │
└──────────────┬──────────────────┘
               │
               │ SQL
               ↓
┌─────────────────────────────────┐
│   PostgreSQL                    │
│   (GCP Cloud SQL)               │
│   - 學校、班級、師生資料          │
│   - 課程、作業、提交記錄          │
│   - 學習歷程、錯誤追蹤            │
└─────────────────────────────────┘
```

---

## 🔑 關鍵決策點

### 為什麼選 FastAPI（Python）而非 NestJS（Node.js）？

| 考量點 | FastAPI (Python) | NestJS (TypeScript) |
|-------|------------------|---------------------|
| **Azure Speech SDK** | ✅ 成熟、文檔完整 | ⚠️ 文檔較少 |
| **AI/ML 整合** | ✅ Whisper、GPT-4、HuggingFace 生態系 | ❌ 需用 child_process 呼叫 Python |
| **文本處理** | ✅ difflib, jellyfish, jieba（中文分詞） | ⚠️ 需額外找 npm 套件 |
| **效能** | ⚠️ 稍慢於 Node.js | ✅ 極快 |
| **全端統一** | ❌ 前後端語言不同 | ✅ TypeScript 統一 |
| **開發速度** | ✅ AI 功能快 | ⚠️ AI 功能慢 |

**結論**：AI/ML 需求 > 全端統一 → 選 FastAPI

---

### 為什麼選 Next.js 14 而非 Remix/Vite？

| 考量點 | Next.js 14 | Remix | Vite + React |
|-------|-----------|-------|--------------|
| **生態系成熟度** | ✅ 最成熟 | ⚠️ 較新 | ⚠️ 需自組 |
| **API Routes** | ✅ 內建 | ✅ 內建 | ❌ 無 |
| **SSR/SSG** | ✅ 強大 | ✅ 優雅 | ⚠️ 需手動配置 |
| **學習曲線** | ⚠️ 稍陡 | ⚠️ 較陡 | ✅ 簡單 |
| **部署** | ✅ Vercel 一鍵部署 | ✅ 支援多平台 | ⚠️ 需手動配置 |

**結論**：成熟生態系 + 全端整合 → 選 Next.js 14

---

## 📂 專案結構

```
chinese-literacy-platform/
├── frontend/                  # Next.js 14 前端
│   ├── app/                   # App Router
│   │   ├── (student)/         # 學生端頁面
│   │   │   ├── lesson/[id]/   # 課程頁面
│   │   │   └── practice/      # 練習頁面
│   │   ├── (teacher)/         # 教師端頁面
│   │   │   ├── dashboard/     # 教師儀表板
│   │   │   ├── classroom/     # 班級管理
│   │   │   └── reports/       # 統計報告
│   │   └── api/               # API Routes (BFF)
│   ├── components/            # React 元件
│   │   ├── AudioRecorder.tsx  # 錄音元件
│   │   ├── FeedbackPanel.tsx  # 回饋介面
│   │   └── ProgressChart.tsx  # 進度圖表
│   ├── lib/                   # 工具函式
│   └── package.json
│
├── backend/                   # FastAPI 後端
│   ├── app/
│   │   ├── api/               # API Endpoints
│   │   │   ├── speech.py      # 語音分析 API
│   │   │   ├── lessons.py     # 課程 API
│   │   │   └── students.py    # 學生 API
│   │   ├── core/              # 核心邏輯
│   │   │   ├── azure_speech.py     # Azure SDK 包裝
│   │   │   ├── text_compare.py     # 文本比對演算法
│   │   │   └── feedback_engine.py  # 回饋生成引擎
│   │   ├── models/            # SQLAlchemy Models
│   │   │   ├── school.py
│   │   │   ├── classroom.py
│   │   │   ├── student.py
│   │   │   ├── lesson.py
│   │   │   └── submission.py
│   │   ├── schemas/           # Pydantic Schemas (API 驗證)
│   │   └── database.py        # 資料庫連線
│   ├── requirements.txt       # Python 依賴
│   └── Dockerfile
│
└── docs/                      # 文件
    └── PRD.md
```

---

## 🚀 部署架構（GCP Cloud Run）

```
┌─────────────────────────────────────┐
│   Cloud Run (Next.js)               │
│   - 前端靜態資源 (SSG)                │
│   - BFF API Routes                  │
│   - 自動擴展 0-N 實例                 │
└──────────────┬──────────────────────┘
               │
               ↓
┌─────────────────────────────────────┐
│   Cloud Run (FastAPI)               │
│   - AI 語音分析                       │
│   - 核心業務邏輯                      │
│   - 自動擴展 0-N 實例                 │
└──────────────┬──────────────────────┘
               │
               ↓
┌─────────────────────────────────────┐
│   Cloud SQL (PostgreSQL)            │
│   - 自動備份                          │
│   - 高可用性（HA）                    │
└─────────────────────────────────────┘
```

**成本估算（50 學生）**：
- Cloud Run (Next.js): $5/月
- Cloud Run (FastAPI): $10/月
- Cloud SQL (db-f1-micro): $7/月
- Azure Speech SDK: $250/月
- **總計**: ~$272/月

---

## 🔧 開發工具

| 工具 | 用途 |
|------|------|
| **pnpm** | Node.js 套件管理（比 npm 快 2-3 倍） |
| **Poetry** | Python 依賴管理（取代 pip + requirements.txt） |
| **Docker Compose** | 本地開發環境（PostgreSQL + Redis） |
| **GitHub Actions** | CI/CD 自動部署 |
| **Prettier** | 程式碼格式化（前端） |
| **Black** | 程式碼格式化（後端） |
| **ESLint** | 前端 Lint |
| **Ruff** | 後端 Lint（比 Flake8 快 100 倍） |

---

## 📦 核心依賴

### Frontend (Next.js)

```json
{
  "dependencies": {
    "next": "^14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tailwindcss": "^3.4.0",
    "@tanstack/react-query": "^5.0.0",  // 資料同步
    "zustand": "^4.5.0",                 // 狀態管理
    "recharts": "^2.10.0",               // 圖表（學習歷程）
    "wavesurfer.js": "^7.7.0"            // 音檔波形顯示
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.0"
  }
}
```

### Backend (FastAPI)

```toml
[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.109.0"
uvicorn = {extras = ["standard"], version = "^0.27.0"}
sqlalchemy = "^2.0.0"
psycopg2-binary = "^2.9.9"           # PostgreSQL 驅動
pydantic = "^2.6.0"
azure-cognitiveservices-speech = "^1.35.0"  # Azure Speech SDK
openai = "^1.12.0"                   # GPT-4 (Phase 3)
difflib = "*"                        # 文本比對（標準庫）
jellyfish = "^1.0.0"                 # 字串相似度
jieba = "^0.42.0"                    # 中文分詞
```

---

## ⚠️ 技術風險與應對

| 風險 | 等級 | 應對方案 |
|------|------|---------|
| **Azure Speech SDK zh-TW 準確率不足** | 🔴 高 | Week 1 立即測試，若 < 90% 切換 Google Speech-to-Text |
| **FastAPI 與 Next.js 整合複雜** | 🟡 中 | 使用 CORS + API Gateway 模式 |
| **Python 效能瓶頸** | 🟡 中 | 使用 async/await + Redis 快取 |
| **Cloud Run 冷啟動延遲** | 🟢 低 | 設定最小實例數 = 1（$5/月） |

---

## 📚 參考資源

### Next.js 14
- [官方文檔](https://nextjs.org/docs)
- [App Router 完整指南](https://nextjs.org/docs/app)

### FastAPI
- [官方文檔](https://fastapi.tiangolo.com/)
- [SQLAlchemy 2.0](https://docs.sqlalchemy.org/en/20/)

### Azure Speech SDK
- [Python SDK 文檔](https://learn.microsoft.com/zh-tw/azure/ai-services/speech-service/quickstarts/setup-platform?pivots=programming-language-python)
- [zh-TW 語音模型](https://learn.microsoft.com/zh-tw/azure/ai-services/speech-service/language-support)

### 部署
- [GCP Cloud Run 文檔](https://cloud.google.com/run/docs)
- [Cloud SQL for PostgreSQL](https://cloud.google.com/sql/docs/postgres)

---

**決策確認**: ✅ Next.js 14 + FastAPI

**Generated with [Claude Code](https://claude.ai/code) via [Happy](https://happy.engineering)**

Co-Authored-By: Claude <noreply@anthropic.com>
Co-Authored-By: Happy <yesreply@happy.engineering>
