# Private notes (strategy, personal context)
private/

# Environment variables (secrets)
venv/
.venv/
.env
.env.local
.env.production

# Dependencies
node_modules/
__pycache__/
*.pyc

# Build outputs
dist/
build/

# Raw teaching materials (1GB+, not for git)
data/raw-materials/

# Test artifacts
*.db
frontend/test-results/
frontend/playwright-report/
frontend/e2e/.auth/
frontend/e2e/screenshots/
frontend/e2e-report.html

# Load test reports (generated artifacts — not committed)
tests/load-testing/reports/
tests/load-testing/.venv/

# Claude worktrees (temporary, per-issue)
.claude/worktrees/

# macOS
.DS_Store
.gstack/
.worktrees/

# Firebase deploy cache
.firebase/

# Transient logs + scratch workspace
logs/
workspace/

# /issue-fix skill outputs — work-progress notes, posted to PRs/issues, not committed
issue*done.md

# Content Evidence Gate (#2397) — re-shootable artifacts (screenshots / api payloads / logs)
# stay local; the *.jsonl + coverage_manifest.json records ARE committed as evidence.
qa/content-evidence/*/artifacts/

# Spotlight→Lesson adapter full-corpus DRY-RUN output (#2205 EDD). Bulk 134× regenerable
# *.lesson.yml stay local; the runner + coverage table + known-gaps ledger ARE committed.
# Regenerate: backend/.venv/bin/python scripts/batch_corpus_dryrun.py --with-keypoints
backend/data/lessons/_lesson_content_dryrun/
# graphify architecture map — local knowledge-graph cache (18M), rebuilt on demand, not committed
graphify-out/
.playwright-mcp/
backend/demo-reading-output/

# QA manifest smoke 產物（只跑 5 課，不是正式輸出，見 #2795）
backend/data/curriculum_qa/keypoints_manifest.smoke.json
backend/data/curriculum_qa/snapshots.smoke/
