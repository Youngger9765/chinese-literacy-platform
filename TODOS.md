# TODOS — LingoLeap Eng Review 2026-04-05

Generated by `/plan-eng-review` full site review on staging branch.

---

## P1 — High Impact

### 1. Persist Socratic SessionStore to DB
- **What**: Replace in-memory `SessionStore` dict with DB-backed storage (add `dialogue_state` JSON column to `LearningSession`)
- **Why**: Cloud Run redeploy/scale loses all Socratic dialogue progress. Students restart from Q1
- **How to apply**: Add nullable `dialogue_state: JSON` column to `LearningSession`, modify `SessionStore.get()`/`save()` to read/write DB
- **Depends on**: Alembic migration (need Young's permission)
- **Effort**: CC ~30 min

### 2. Frontend test coverage — top 5 components + AuthContext
- **What**: Write tests for LiveTutor, ComprehensionChat, AssessmentReport, VocabDefinitionMatch, AuthContext
- **Why**: 7% frontend test coverage (16/218 files). Largest components have zero tests
- **How to apply**: Create `__tests__/` files for each, test happy path + key interactions
- **Depends on**: Vitest setup (check if vitest.config exists)
- **Effort**: CC ~1 hour

### ~~3. Add pagination to teacher analytics + learning errors~~ ✅ DONE
- teacher_analytics: added `.limit(5000)` safety cap on unbounded session queries
- learning_errors: added `limit`/`offset` Query params to `get_error_patterns` and configurable `limit` to `get_recommended_vocab`

## P2 — Medium Impact

### ~~4. Extract seed data from main.py~~ ✅ DONE
- Moved to `services/seed.py`. main.py: 819 → 340 lines

### ~~5. Remove dead TenantMiddleware~~ ✅ DONE
- Deleted `middleware/tenant.py` + `tests/test_tenant_middleware.py` + removed from main.py

### ~~6. Split LiveTutor.tsx into sub-components~~ ✅ DONE
- Extracted to `reading-steps/live-tutor/`: helpers.ts (134), FeedbackPanel.tsx (129), ParagraphCard.tsx (341), LiveTutor.tsx (720), index.tsx (2)
- Original LiveTutor.tsx is now a 6-line re-export shim for backward compatibility

### ~~7. Narrow bare `except Exception` catches in AI/learning paths~~ ✅ DONE
- ai_base.py: `_repair_json` narrowed to `(json.JSONDecodeError, ValueError)`
- socratic_agent.py: DB session restore → `(SQLAlchemyError, AttributeError, KeyError)`, DB delete → `SQLAlchemyError`
- 10 catches left as `except Exception` — intentional fail-safes around external AI calls

## P3 — Low Priority (noted, no action needed now)

### 8. Sync SQLAlchemy on async event loop
- **What**: 239 sync endpoints block the event loop thread
- **Why**: FastAPI thread pool (40) handles this, but could exhaust under heavy load
- **When to act**: When you see slow responses under concurrent load (50+ students)
- **No action needed now**
