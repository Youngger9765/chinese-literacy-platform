fastapi>=0.115,!=0.136.3  # 0.136.3 contains malicious typosquat dep injection (MAL-2026-4750, #2002)
uvicorn[standard]>=0.30
pydantic>=2.0
email-validator>=2.0
pydantic-settings>=2.0
# ⛔ 上限是必要的，不是保守。SQLAlchemy 2.1.0（2026-09-24 20:36 發佈）把
# `postgresql` scheme 的預設 DBAPI 從 psycopg2 換成 psycopg(v3)，而這個 image
# 只裝 psycopg2-binary → 容器啟動時 alembic 就 `ModuleNotFoundError: No
# module named 'psycopg'` 然後 exit(1)。2026-09-25 所有新建的 preview 後端
# 全數起不來就是這個（#3320）；prod/staging 只是還沒重建 image 而已。
# 要升 2.1 就得一起換 psycopg3 並驗過 —— 那是獨立的一件事，不是隨 build 漂移。
sqlalchemy>=2.0,<2.1
psycopg2-binary>=2.9
redis>=5.0
python-dotenv>=1.0
pypinyin>=0.51
jieba>=0.42
pytest>=8.0
pytest-asyncio>=0.23
# schema 驗證（#2865 的門與測試都用它）。原本靠別的套件間接帶進來，
# 本機有、CI 沒有 —— 那種相依不宣告就會在 CI 才炸。
jsonschema>=4.0
httpx>=0.27
# Azure TTS is called through requests, not urllib: urllib fails ~12% of the
# time on Azure's chunked responses (IncompleteRead), and each failure used to
# become mainland-accent audio via the Google fallback. Listed explicitly
# because it arrives here only as a transitive dependency today.
requests>=2.31
google-genai>=1.0
alembic>=1.13
google-cloud-storage>=2.14
pyyaml>=6.0
pyjwt>=2.8
google-auth>=2.28
bcrypt>=4.0
python-multipart>=0.0.9
python-json-logger>=2.0.0
slowapi>=0.1.9
sentry-sdk[fastapi]>=2.0
google-cloud-texttospeech>=2.14
pypdfium2>=4.30
Pillow>=10.0
qrcode[pil]==8.2
